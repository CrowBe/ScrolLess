Place your PWA icons here:
  icon-192.png  — 192x192 pixels
  icon-512.png  — 512x512 pixels

These are referenced by src/manifest.json and the service worker.
Until real icons are added, the PWA install prompt and push notifications
will use a placeholder (browser default).
